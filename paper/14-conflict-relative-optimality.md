# 14. Conflict-Relative Optimality

There may be no neutral single best address representation in disputed,
multilingual, multi-institutional, or multi-purpose contexts.

AMT therefore defines optimality relative to context, purpose, source policy,
and display policy. The resolver should expose the policy used rather than hide
the conflict.

Model hook: `formal/equivalence-classes.ts`.
