# 23. Benchmarks And Comparison

AMT should be compared against string normalization, geocoding, postal lookup,
commercial validation APIs, address translation, and identity systems.

The correct benchmark is not only accuracy on clean addresses. It must include
abstention quality, candidate recall, unresolved states, source transparency,
privacy leakage, and history preservation.

Model hook: `formal/verification-map.ts`.
