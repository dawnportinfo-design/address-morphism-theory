# 19. PID And Application Identifier Boundary

A PID identifies the AMT referent under a source and lineage policy. Application
identifiers support specific workflows such as delivery receipt, POS handoff,
hotel check-in, locker pickup, or field operations.

Application identifiers must not silently redefine the underlying referent.

Model hook: `diagrams/pid-boundary.mmd`.
